<?php

declare(strict_types=1);

namespace App\Blog\Domain\Event;

/**
 * Маркер доменного события.
 *
 * Метода времени здесь намеренно нет: ни один обработчик его сейчас не читает,
 * а взять достоверное «сейчас» в домене нельзя без отдельного порта часов.
 * Появится потребитель — появится и метка времени вместе с Clock.
 */
interface DomainEvent
{
}
